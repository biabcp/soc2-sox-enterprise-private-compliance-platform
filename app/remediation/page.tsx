import { remediations } from '@/lib/data';
export default function P(){return <div className='space-y-2'><h2 className='text-2xl'>Remediation Tracker</h2><div className='card'>Kanban summary: {['Not Started','In Progress','Blocked','Complete','Accepted Risk'].map(s=>`${s} ${remediations.filter(r=>r.status===s).length}`).join(' | ')}</div><div className='card overflow-auto'><table className='w-full text-sm'><thead><tr><th>ID</th><th>Title</th><th>Status</th><th>Owner</th><th>Due</th></tr></thead><tbody>{remediations.map(r=><tr key={r.id} className='border-t'><td>{r.id}</td><td>{r.title}</td><td>{r.status}</td><td>{r.owner}</td><td>{r.dueDays}</td></tr>)}</tbody></table></div><a href='/api/export/remediation' className='text-blue-600'>Export CSV</a></div>}
